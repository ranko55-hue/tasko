# DESIGN.md — חוקת העיצוב של Tasko

כמו `CONVENTIONS.md`, רק לעיניים. **כל מסך חדש נבחן מול הקובץ הזה.**
מקור ההשראה המחייב: הדמו שאושר ע"י השותפים (`tasko-demo.html`).

## 1. הזהות: שני עולמות, מערכת אחת

- **מנהל = מגדל פיקוח 🗼** — כהה, צפוף, נתונים חיים. רקע כהה בכותרות, הדגשות צהובות.
- **עובד = שטח 👷** — בהיר, ענק, חד. חייב להיות קריא בשמש ישירה ולחיץ עם כפפות.

## 2. טוקנים (חד-משמעי — אין להמציא צבעים)

```
navy    #0F172A   רקע כהה ראשי (כותרות מנהל, כפתור ראשי כהה)
navy2   #1E293B   כהה משני (פאנלים על כהה)
yellow  #FACC15   ההדגשה של המותג — לוגו, דירוגים, פעולות משניות עובד
green   #22C55E   בעבודה / התחל / הצלחה
red     #EF4444   סיים / חריגה / עיכוב
blue    #3B82F6   מידע / עתידי / קישורים
bg      #F1F5F9   רקע אפליקציה
card    #FFFFFF   כרטיסים
line    #E2E8F0   מסגרות
gray    #64748B   טקסט משני
ink     #0F172A   טקסט ראשי
```

- **פונט:** Heebo בלבד. משקלים: 400 גוף, 700 מודגש, 900 כותרות וכפתורים.
- **עיגולים נדיבים:** כרטיסים 16–18px, כפתורים 12–16px, צ'יפים 99px.
- **צללים רכים בלבד** (בלי מסגרות שחורות קשות).
- **מספרים רצים (טיימרים):** `font-variant-numeric: tabular-nums`.

## 2.1 לוגו

הלוגו הרשמי: **TASKO** — wordmark ב-navy עם נקודה צהובה (yellow `#FACC15`).

קבצי המותג ב-`public/brand/`:

| קובץ | שימוש |
|---|---|
| `tasko-header-light.png` | כותרת על **רקע בהיר** (wordmark navy) — גובה ~28px |
| `tasko-header-dark.png` | כותרת על **רקע כהה** (wordmark לבן) — לזהות המנהל 🗼 |
| `favicon-32.png` | favicon בדפדפן |
| `icon-180.png` | apple-touch-icon |
| `icon-192.png`, `icon-512.png` | אייקוני PWA (`manifest.json`) |

**כלל ברזל:** הלוגו הוא נכס קבוע — **אין לעוות, למתוח, לצבוע מחדש, או להוסיף אפקטים** (צל, מסגרת, זוהר). משתמשים בקובץ המתאים לרקע כמו שהוא, גובה בלבד משתנה.

## 3. חוקת הכפתורים (מסך עובד — מחייב, מהאפיון 3.2)

1. **פעולה ראשית אחת לכל מצב** — רוחב מלא, גובה 64px+, פונט 20px+.
2. **פעולה משנית** — רוחב מלא, סגנון מסגרת. לעולם לא שני חצאים זה לצד זה.
3. **אף מטרת לחיצה מתחת ל-48px.** כולל התראות וצ'יפים. זהו המינימום בכל
   המערכת — עובד ומנהל, נייד ודסקטופ (הוכרע 31.07.2026).
4. **רשימות מכווצות:** לא-פעיל = שורת כותרת; פעיל נפתח אוטומטית. הכפתור הראשי תמיד מעל קו המסך.

### צבע לפי מדרג הפעולה

| מדרג | צבע | היכן |
|---|---|---|
| **פעולה ראשית של המסך** | צהוב המותג `#FACC15` + טקסט `navy` | "משימה חדשה" בלוח, בכרטיס הלקוח ובמגירת המובייל |
| **פעולה בתוך כרטיס** | כחול `brand` + טקסט לבן | "שייך לעובד", "טפל עכשיו", "עדכון לעובד" |
| **פעולה משנית** | מסגרת, ללא מילוי | "המשימות שלי", "ערוך", "ביטול" |
| **פעולה הרסנית** | מסגרת אדומה על רקע לבן | "ביטול משימה" |

הצהוב שמור לפעולה הראשית **אחת** בכל מסך. שני כפתורים צהובים באותו מסך = הפרה.
בתוך כרטיס אין צהוב כלל — אחרת הכרטיסים מתחרים עם הפעולה הראשית של המסך.

### שדה חיפוש בפס ה-navy

שדה מעוגל מאופק בתוך הפס: רקע `bg-white/10`, טקסט לבן, placeholder `slate-400`,
מיקוד מבהיר ל-`bg-white/20`. אייקון זכוכית מגדלת בתוך השדה, לא לצידו.
**ב-390 השדה מכווץ לאייקון בלבד** ומתרחב בלחיצה — הפס צר מדי לשדה קבוע.
חיפוש אחד גלובלי בלבד, בפס, זמין מכל מסך. אין שדה חיפוש שני בגוף מסך.

### שורת טבלה (מסכי ריכוז)

טבלה מותרת **רק בדסקטופ**. ב-390 כל שורה הופכת לכרטיס דחוס: שם למעלה,
מונים בשורה אחת מתחתיו. **טבלה לעולם לא נגללת אופקית** — היא מתחלפת.
לחיצה על השורה = כניסה לישות. פעולה בקצה השורה חייבת `stopPropagation`.
מספרים בטבלה תמיד `tabular-nums`; מונה התראה נצבע רק כשהוא גדול מ-0
(1–2 כתום, 3+ אדום) — אפס נשאר אפור ולא מושך את העין.

### מגירת המשימה — טוקנים (v2)

| שימוש | טוקן | ערך |
|---|---|---|
| רקע כותרת המגירה | — | `linear-gradient(135deg, #0F172A, #1E293B)` |
| צ'יפ דחיפות | `urgentSoft` / `urgentInk` | `#FEF2F2` / `#B42318` |
| צ'יפ יעד | `dueSoft` / `dueInk` | `#FEF9C3` / `#854D0E` |
| צ'יפ דרישות | `reqSoft` / `reqInk` | `#EEF2FF` / `#3730A3` |
| קופסת חריגה | `overrunSoft` / `overrunLine` | `#FFFBFA` / `#F5DAD7` |
| רקע שורת פעולות | `surfaceBar` | `#FBFCFD` |
| פעימת סטטוס חי | `animate-softPulse` | opacity, 2s |

**כללים:**

- **צ'יפ מוצג רק אם יש לו ערך.** אין צ'יפ ריק ואין מציין־מקום.
- **פעימה = חי בלבד.** הנקודה פועמת רק כשהסטטוס פעיל והטיימר רץ. מצב לא-פעיל ניטרלי לגמרי.
- **חריגה לעולם לא שלילית.** החישוב ב-`lib/taskTime` נגזם לאפס: השדה `overrun_alerted`
  נשאר דלוק גם אחרי שהמוקצב מוגדל בעריכה, ובלי הגזימה המסך הציג "‎180- דקות מעל".
- **ספרות זמן וכמות תמיד `tabular-nums`.**
- **גדלים מהמוקאפ נשמרים ויזואלית**: ממוזערת רצועה 58px, ממוזערת ציר 26px.
  כשהגודל הנדרש קטן מ-44px, מרחיבים את **אזור הלחיצה** בריפוד ולא את התמונה.
- **נגן שמע מותאם** (`components/media/AudioPlayer`) מחליף את נגן הדפדפן בכל מקום
  שמופיעה הקלטה — הוא יושב ב-`EventMedia` המשותף ולכן משתדרג בכל המסכים יחד.
- **הערת מנהל מוצגת במירכאות** בציר הזמן, ונקודתה צהובה. אירועי עובד — ירוקים.

## 4. צבעי סטטוס (עקביים בכל המערכת — מנהל ועובד)

ממתין = אפור · בביצוע = ירוק (+נקודה פועמת) · מושהה = צהוב · מעוכב/חריגה = אדום · הושלם = אפור-כהה שקוף · מתוזמן = כחול

## 5. מצבי מסך — אין מסכים מתים

כל מסך מגדיר שלושה מצבים:

- **ריק:** אייקון גרפי (`components/ui/Icon`) + משפט חם + פעולה מוצעת. אף פעם לא לבן ריק.
  **אין אימוג'י בשום מקום במערכת** — לא בטקסט, לא בכפתורים ולא במצבי ריק.
  הכלל הזה גובר על כל ניסוח אחר במסמך (הוכרע 31.07.2026). גם גליפים
  טקסטואליים כמו ☰ / ⌃ / ✓ נחשבים הפרה — משתמשים ב-`ui/Icon`.
- **טוען:** שלד (skeleton) או ספינר ממותג — לא קפיאה.
- **שגיאה:** מה קרה בעברית פשוטה + מה עושים ("נסה שוב").

## 6. עברית ושטח

- **RTL תמיד.** תאריכים ושעות בפורמט ישראלי (14:30, לא 2:30 PM).
- **טקסט מינימלי לעובד:** פעלים בציווי קצר ("התחל עבודה", "סיים משימה").
- **ניגודיות:** טקסט על צבע — לבן או navy בלבד; אין אפור-על-אפור מתחת ל-14px.

## 7. אסור

- ❌ כפתור ראשי בחצי רוחב
- ❌ אייקון בלי מילה בפעולות ראשיות
- ❌ צבע מחוץ לטוקנים
- ❌ מודאל שנפתח על מודאל
- ❌ טקסט מערכת באנגלית מול משתמש

## 8. פריסה ומבנה (שכבת ה-UI המשותפת)

**שני עקרונות מחייבים (מסך הייחוס: `/dashboard`):**

1. **הפס השחור העליון הוא זהות המערכת** — פס navy קבוע, **זהה בכל מסך ובכל רוחב** (`AppShell`). לוגו, ניווט, חיפוש, פעולות, משתמש.
2. **מסך אחד, שני מצבים** — אותו תוכן ואותן פעולות בדסקטופ ובנייד; **משתנה רק הצפיפות**. אין פיצ'ר שקיים רק בדסקטופ, ואין "גרסת מובייל" נפרדת. (דוגמה: הקנבן = 4 טורים בדסקטופ, צ'יפים+רשימה בנייד — אותן משימות ואותן פעולות.)

כל המסכים נבנים מ**שכבת פרימיטיבים אחת** ב-`src/components/ui/`. אין להמציא מבני עמוד חדשים.

**הפרימיטיבים:**

| רכיב | תפקיד |
|---|---|
| `AppShell` | פס navy קבוע (זהות המערכת) + מגירת מובייל + `PageShell`. עוטף כל מסך מחובר. |
| `PageShell` | מיכל עמוד — רוחב מקסימלי אחיד, ריפוד אחיד, רקע `bg`. מסופק ע"י `AppShell`. |
| `PageHeader` | כותרת + שורת משנה אפורה + אזור פעולות מימין. מבנה זהה בכל מסך. |
| `Card` | הכרטיס הלבן הסטנדרטי — עיגול, מסגרת `line`, צל רך. |
| `Row` | **שורת המערכת** — אייקון + ראשי + משני + תג בקצה + לחיץ. השורה של כל המערכת (משימות/פרויקטים/מסמכים/לקוחות/עובדים). |
| `Tabs` | לשוניות פנימיות (מחליפות תוכן, לא מנווטות), פעילה בצהוב. |
| `StatusPill` | תג סטטוס — צבעים **אך ורק** מסעיף 4. `STATUS_TONE` ממפה סטטוס→גוון. |
| `EmptyState` | **אייקון מ-`ui/Icon`** + משפט + פעולה מוצעת (סעיף 5). אין אימוג'י — גם לא במצב ריק. |
| `Field` | שדה טופס אחיד — תווית, קלט, שגיאה. |

**כללי פריסה (חלים על כולם):**

- **סולם מרווחים אחד בלבד:** 4 · 8 · 12 · 16 · 24 · 32 px. אין ערכי ביניים.
- **נקודת שבירה אחת: `md`.** מובייל-first — עמודה אחת; במסך רחב רשת 2 עמודות היכן שיש טעם.
- **טבלה מותרת בדסקטופ.** אותם נתונים במובייל = כרטיסים, לא טבלה נגללת.
  טבלת הלקוחות (`ClientTable`) היא הסטנדרט המחייב, לא חריגה. הפירוט בסעיף 3.
- **אף מטרת לחיצה מתחת ל-48px** — גם בדסקטופ. (`min-h-touch` בטיילווינד.)
- **כותרת עליונה קבועה בכל המסכים** — אותו `AppShell`, כולל מסכי המנהל.

> **כלל ברזל:** אסור לבנות מסך חדש בלי `AppShell` + `PageShell` + `PageHeader`.

## 9. פעולה במקום (במקום ניווט לעמוד חדש)

פעולה נפוצה נעשית **ישירות מהכרטיס**, לא בניווט לעמוד אחר. פרטים בעלי ערך (עריכה, מוצא קול, זמן) נפתחים במגירה מעל המסך הנוכחי:

- **כרטיס משימה** — לחיצה פותחת `TaskDrawer` (ניתן לצפייה ועריכה).
- **פעולות בלוח:** "＋ שייך", "💬 עדכון", "✏️ ערוך" — בלי לעזוב את הלוח.
- **נתיב ישיר `/tasks/:id`** — פותח את אותה מגירה מעל המסך הנוכחי (לא עמוד).
- **מגירה (Drawer):** נייד = bottom sheet; דסקטופ = side panel. סגירה ברקע/Esc/החלקה.

## 10. דשבורד (מגדל הפיקוח) — מסך הייחוס של המערכת

**דפוס בסיסי:** פס עליון קבוע + פס AI + שורת פעולות + גריד כרטיסים. בנייד: טור אחד, אותה הגדרות.

### א. פס עליון (AppShell עדכון)

- **ניווט:** לוח | לקוחות | פרויקטים | הגדרות (פעיל מודגש / קו תחתון צהוב, `text-yellow` + `underline`)
- **שדה חיפוש רחב:** "חיפוש משימה, לקוח או פרויקט…" — לוקח את הרוחב הפנוי בין הניווט למימין
- **"המשימות שלי":** הוסר מניווט, מועבר לשורת פעולות (סעיף ג)

### ב. מודול "תדריך AI — חי" (החלף KPI row + banner קריאות)

- **רקע:** Navy קבוע (`#0F172A`), אף פעם לא משנה צבע
- **כותרת:** 🤖 "תדריך AI — חי" + בקצה: 🔔 + מונה אדום + חץ ⌃ (סגור) / ⌄ (פתוח)
- **שורת צ'יפים (חכמה ב-scroll):**
  - **צ'יפים אדומים (אם count > 0):** 🚨 קריאות חדשות (N) · ⏸ בעיכוב (N) · 🔥 בחריגה (N) · ⚠️ לא נסגרה בזמן (N)
  - **ברירת מחדל (אם כל count = 0):** ✅ הכל תקין (ירוק יחיד)
  - **צ'יפים מוצמדים (אפורים, עד 3):** בחירת המנהל מתוך:
    🟢 בשטח כרגע · ⏱ בהשהיה · 📋 טרם החלו · 📅 מתוזמנות · ✔️ הושלמו היום
  - **כפתור ＋ בסוף:** פותח בורר להצמדה/הסרה
  - **אם לא נכנס:** "+N" (צ'יפ אפור) פותח את הפאנל
- **פאנל פתוח (תחת השורה):**
  - רשימת כל הפריטים ב**סדר כניסה (הוותיק למעלה)**
  - כל שורה: אייקון סוג (🚨/⏸/🔥/⚠️) + "כותרת — עובד" + סיבה (אם יש) + שעת כניסה + כפתור פעולה
  - כפתורים: "פתח" (למשימה) / "המר למשימה" (לקריאת לקוח)
  - **אין דירוג עדיפות, אין מספור**
- **ברירת מחדל:** מקופל. מצב הקיפול + צ'יפים מוצמדים נשמרים ב-localStorage

### ג. שורת פעולות

- "＋ משימה חדשה" (כחול מלא, 64px, טקסט 18px) + "המשימות שלי" (משני, מסגרת)

### ד. כרטיס משימה (עיצוב חדש)

- **מצע:** לוח רקע קצת כהה יותר מהיום; כרטיס לבן, 1px border line, `rounded-[10px]`
- **שורה עליונה:** 
  - נקודת סטטוס (7px) + שם סטטוס **bold 12.5px בצבע הסטטוס** (לא טקסט אפור) + **מספר משימה קריא בקצה** (ספרות tabular-nums)
- **כותרת:** גוף המשימה (bold)
- **שורת לקוח:** "לקוח · פרויקט" (אפור)
- **שורת נתונים (מופרדת בקווים דקים):** יעד | מוקצב | דחיפות
  - **כל הספרות:** `font-variant-numeric: tabular-nums`
- **פס ניצול זמן (אם `est_minutes` > 0):**
  - טקסט למעלה: "0:42 / 2:00" (left-aligned, tabular)
  - פס 4px מתחת: 
    - ירוק עד 85% (NET ≤ 0.85 × est)
    - כתום 85–100% (NET ≤ est)
    - אדום > 100% (overrun)
    - אפור כשהמשימה `paused`
  - שורת סטטוס מתחת (12px):
    - ✅ "תקין · 35%" (ירוק)
    - ⚠️ "מתקרב ליעד · 92%" (כתום)
    - 🔥 "חריגה · 24 דקות מעל" (אדום)
    - ⏸ "הזמן עצור · הפסקה" (אפור)
- **תחתית:** אווטאר (32px) + שם העובד + טיימר חי (tabular)
- **מצב מעוכב (`status: 'blocked'`):**
  - רקע ורוד עדין מאוד (לא תיבה אדומה)
  - ציטוט העובד בטיפוגרפיה קלה (קו דק מימין, לא סיכום)
- **כפתור פעולה:**
  - **כחול מלא רק כשדורש פעולה:**
    - אין עובד → "＋ שייך לעובד"
    - מעוכב → "🔧 טפל עכשיו"
  - **כרטיסים שקטים אחרת** (אין כפתור בולט)

### ה. פתיחת פרטים בתוך הכרטיס

- **בתחתית כל כרטיס:** "📋 פרטים ועדכונים ⌄"
- **לחיצה:** נפתח בתוך הכרטיס (לא ניווט)
  - ממוזערות תמונות (לחיצה מגדילה)
  - 🎵 נגן להערה קולית
  - **3 העדכונים האחרונים בלבד:**
    - טקסט + שעה + שם כותב
    - נקודה צהובה = עדכון מנהל
    - נקודה ירוקה = פעולת עובד
- **מתחת:** "💬 עדכון לעובד" (כחול) · "✏️ ערוך" (משני) · 🔗 אייקון לפתיחת מסך מלא (עם כל ההיסטוריה)

### ו. גריד כרטיסים (דסקטופ 1280px)

- 4 טורים (like Kanban today) — אך לא קנבן, פשוט רשימות כרטיסים
- גובה כרטיסים אחיד (דינמי לתוכן)
- מרווחים 16px בין כרטיסים

### ז. נייד (390px)

- טור אחד, רוחב מלא (מינוס padding)
- שורת צ'יפים נגללת אופקית (אותה שורה, לא נשברת)
- כרטיסים ברוחב מלא
- כל מטרת לחיצה ≥44px

## 11. גלי עיצוב (תיאום ציפיות)

- **גל 1 (עכשיו):** כל מסך חדש נבנה לפי קובץ זה.
- **גל 2:** ליטוש פר-מסך אחרי שהוא עובד פונקציונלית — סקירה בטלפון אמיתי.
- **גל 3 (לפני פיילוט):** אנימציות מיקרו, מסכי ריק/טעינה בכל המערכת, אייקון PWA + splash, בדיקת שמש-וכפפות בשטח אמיתי.

## 12. TaskDrawer v8 — טוקנים וקידוד צבע (מוקאפ מאושר)

**פלטת המגירה** (tailwind: `dr*`): navy `#0f2a43` · navy-deep `#0a1f33` · צהוב `#f5c518` · ירוק `#188a4e` · אדום `#c53030` · כחול `#2b5fa8` · שחור-דיו `#161d24` · כתום `#d97c0e` · קו `#c9cfd6`. radius אחיד 8px.

**קידוד צבע קבוע (חוק):**
- **כחול** = תיעוד חזותי / מידע (כתובת, מדיה, צילום, אירועי תיעוד בציר).
- **ירוק** = קול חי / סטטוס פעיל / אישור (טיימר רץ, הקלטה, "רץ כעת", אירועי התחלה/סטטוס).
- **שחור-דיו** = כתיבה / נתונים (הערה כתובה, פרטי משימה).
- **צהוב** = זהירות / השהיה / זמן (רצועת יעד, הנחיות, דרישות, כפתור הפסקה).
- **אדום** = עצירה / סיום (בלוק עצירה-וסיום, כפתור סיום, חריגה).
- אפור = אירועי מערכת בציר הזמן.

**איסור:** בלי אימוג'י בחלונית — אייקוני SVG קו (stroke 2) בלבד.

## 13. שדות מותאמים — ירושת עיצוב (חוק)

שדה מותאם יורש **תמיד** את עיצוב השדות המובנים של הישות שבה הוא יושב — אותו רכיב, אותם קלאסים, אותו גריד. אין לו עיצוב משלו:
- **כרטיס לקוח** (לשונית כללי): אריח זהה לשדות המובנים (רקע אפרפר, תווית קטנה, ערך מודגש), ממשיך את אותו גריד דו-עמודתי. ריק → "—".
- **מודאל/מגירת משימה**: אותו קלט/תצוגה כשל שדות המשימה המובנים.
- הפרדה מותרת רק ככותרת מקטע טקסטואלית קטנה (11px, אפור, letter-spacing) — בלי קונטיינר/קופסה נפרדת.
