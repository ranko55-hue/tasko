# כללי עבודה (CONVENTIONS)

קובץ זה הוא החוק. כל קוד שנכנס לפרויקט עומד בכללים האלה — בלי יוצאים מהכלל.

## 1. גודל קבצים

- **אף קובץ לא עובר ~300 שורות.** קובץ שגדל — מפוצל לרכיבים/מודולים.
- אין "קובץ אחד גדול שמכיל הכל". לעולם.

## 2. שמות

| סוג | פורמט | דוגמה |
|---|---|---|
| רכיב React | PascalCase.jsx | `TaskCard.jsx`, `ClientList.jsx` |
| Hook | use + camelCase.js | `useTasks.js`, `useOrgMembers.js` |
| קובץ עזר | camelCase.js | `formatDate.js`, `supabase.js` |
| מיגרציית SQL | NNN_snake_case.sql | `003_clients.sql` |
| תיקייה | kebab-case או מילה אחת | `components/tasks/` |

## 3. מיגרציות SQL

- ממוספרות ברצף: `001_`, `002_`, `003_`...
- **לעולם לא עורכים מיגרציה שכבר הורצה בפרודקשן** — מוסיפים מיגרציה חדשה שמתקנת.
- כל מיגרציה שהורצה ב-Supabase SQL Editor נשמרת מיד כקובץ ב-`supabase/migrations/`. ה-repo הוא המקור האמין של הסכמה.
- כל טבלה נוצרת עם RLS מופעל מהרגע הראשון.

## 4. Git

- הודעות commit בעברית, קצרות וברורות: `הוספת מסך משימות לעובד`, `תיקון RLS בטבלת tasks`.
- commit אחד = נושא אחד. לא מערבבים תיקון באג עם פיצ'ר חדש.
- פיצ'ר גדול → branch נפרד → merge ל-main כשעובד.

## 5. טקסטים ושפה

- **אף מחרוזת עברית לא נכתבת בתוך רכיב.** הכל ב-`src/locales/he.js`.
- מפתחות תיאוריים: `tasks.newTask`, `clients.addClient` — לא `text1`, `label7`.

## 6. רכיבים

- רכיב = אחריות אחת. `TaskCard` מציג משימה; הוא לא טוען נתונים ולא מנהל מודאלים.
- טעינת נתונים — רק דרך hooks (`useTasks`), לא ישירות ברכיבים.
- רכיבים משותפים (כפתור, מודאל, שדה) — ב-`components/shared/` בלבד.

## 7. תיעוד החלטות

- כל החלטה ארכיטקטונית / עסקית / עיצובית מהותית → שורה ב-`docs/03-decisions.md`:
  תאריך | ההחלטה | הסיבה.

## 8. אבטחה

- שום מפתח סודי (service_role, API keys) לא נכנס לקוד — רק ל-Environment Variables ב-Vercel/Supabase.
- ה-anon key מותר בקוד לקוח (זה ייעודו), אך כל ההגנה נשענת על RLS.
